# Notas:
Primer programa node:

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base para la tabla de multiplicar    [número] [requerido]
  -l, --list     Muestra la tabla en consola         [booleano] [defecto: false]
  -h, --height   Es la cantidad de iteraciones            [número] [defecto: 10]
  ```